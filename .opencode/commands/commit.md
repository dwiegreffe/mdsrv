---
description: Commit latest changes
---

Review the current changes.

If they belong together, commit them with a concise message.

If they represent multiple logical changes, suggest splitting them into separate commits and ask the user how to proceed.

If the commit is related to an issue, include a brief reference to that issue in the commit message.

When everything is clear commit the changes.
