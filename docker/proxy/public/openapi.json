{
  "openapi": "3.0.0",
  "info": {
    "title": "MDsrv API",
    "version": "1.0.0",
    "description": "Unified public API for modular session, trajectory, topology, and YAML services."
  },
  "tags": [
    { "name": "Session" },
    { "name": "Trajectory" },
    { "name": "Topology" },
    { "name": "YAML" }
  ],
  "paths": {
    "/api/v1/session": {
      "get": {
        "tags": ["Session"],
        "summary": "List stored sessions",
        "responses": { "200": { "description": "Session list", "content": { "application/json": {} } } }
      },
      "post": {
        "tags": ["Session"],
        "summary": "Upload a Mol* session ZIP",
        "parameters": [
          { "name": "name", "in": "query", "required": false, "schema": { "type": "string" } },
          { "name": "description", "in": "query", "required": false, "schema": { "type": "string" } },
          { "name": "source", "in": "query", "required": false, "schema": { "type": "string" } },
          { "name": "version", "in": "query", "required": true, "schema": { "type": "string" } }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/zip": { "schema": { "type": "string", "format": "binary" } }
          }
        },
        "responses": { "200": { "description": "Upload complete" } }
      }
    },
    "/api/v1/session/{id}": {
      "get": {
        "tags": ["Session"],
        "summary": "Download a stored session",
        "parameters": [
          { "name": "id", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": { "200": { "description": "Session ZIP", "content": { "application/zip": {} } } }
      },
      "delete": {
        "tags": ["Session"],
        "summary": "Delete a stored session",
        "parameters": [
          { "name": "id", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": { "200": { "description": "Deleted" } }
      }
    },
    "/api/v1/trajectory": {
      "get": {
        "tags": ["Trajectory"],
        "summary": "List stored trajectories",
        "responses": { "200": { "description": "Trajectory list", "content": { "application/json": {} } } }
      },
      "post": {
        "tags": ["Trajectory"],
        "summary": "Fetch and store an XTC trajectory from a URL",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "required": ["url", "id"],
                "properties": {
                  "url": { "type": "string" },
                  "id": { "type": "string" },
                  "name": { "type": "string" },
                  "description": { "type": "string" },
                  "source": { "type": "string" }
                }
              }
            }
          }
        },
        "responses": {
          "201": { "description": "Created" },
          "400": { "description": "Invalid request" },
          "409": { "description": "Id already exists" }
        }
      }
    },
    "/api/v1/trajectory/{id}": {
      "get": {
        "tags": ["Trajectory"],
        "summary": "Read a stored XTC trajectory file",
        "parameters": [
          { "name": "id", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": { "200": { "description": "XTC content", "content": { "application/octet-stream": {} } } }
      },
      "put": {
        "tags": ["Trajectory"],
        "summary": "Upload an XTC trajectory directly",
        "parameters": [
          { "name": "id", "in": "path", "required": true, "schema": { "type": "string" } },
          { "name": "name", "in": "query", "required": false, "schema": { "type": "string" } },
          { "name": "description", "in": "query", "required": false, "schema": { "type": "string" } },
          { "name": "source", "in": "query", "required": false, "schema": { "type": "string" } },
          { "name": "fileName", "in": "query", "required": false, "schema": { "type": "string" } }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/octet-stream": { "schema": { "type": "string", "format": "binary" } }
          }
        },
        "responses": {
          "201": { "description": "Created" },
          "400": { "description": "Invalid request" },
          "409": { "description": "Id or file already exists" },
          "413": { "description": "Upload exceeds configured limit" }
        }
      },
      "delete": {
        "tags": ["Trajectory"],
        "summary": "Delete a stored trajectory",
        "parameters": [
          { "name": "id", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": { "200": { "description": "Deleted" } }
      }
    },
    "/api/v1/trajectory/{id}/starts": {
      "get": {
        "tags": ["Trajectory"],
        "summary": "Get trajectory frame start offsets",
        "parameters": [
          { "name": "id", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": { "200": { "description": "Frame starts", "content": { "text/plain": {} } } }
      }
    },
    "/api/v1/trajectory/{id}/frame/offset/{start}/{end}": {
      "get": {
        "tags": ["Trajectory"],
        "summary": "Get exactly one trajectory frame by start offset and exclusive end boundary",
        "parameters": [
          { "name": "id", "in": "path", "required": true, "schema": { "type": "string" } },
          { "name": "start", "in": "path", "required": true, "schema": { "type": "string" } },
          { "name": "end", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": { "200": { "description": "Single-frame payload", "content": { "application/json": {} } } }
      }
    },
    "/api/v1/trajectory/{id}/frame/start/{start}": {
      "get": {
        "tags": ["Trajectory"],
        "summary": "Get exactly one trajectory frame by frame start offset",
        "parameters": [
          { "name": "id", "in": "path", "required": true, "schema": { "type": "string" } },
          { "name": "start", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": { "200": { "description": "Single-frame payload", "content": { "application/json": {} } } }
      }
    },
    "/api/v1/trajectory/{id}/frame-range/offset/{start}/{end}": {
      "get": {
        "tags": ["Trajectory"],
        "summary": "Get all complete trajectory frames in an exclusive byte range",
        "parameters": [
          { "name": "id", "in": "path", "required": true, "schema": { "type": "string" } },
          { "name": "start", "in": "path", "required": true, "schema": { "type": "string" } },
          { "name": "end", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": { "200": { "description": "Multi-frame payload", "content": { "application/json": {} } } }
      }
    },
    "/api/v1/topology": {
      "get": {
        "tags": ["Topology"],
        "summary": "List stored PDB topology entries",
        "responses": { "200": { "description": "Topology entries", "content": { "application/json": {} } } }
      },
      "post": {
        "tags": ["Topology"],
        "summary": "Fetch and store a PDB topology from a URL",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "required": ["url", "id", "fileName"],
                "properties": {
                  "url": { "type": "string" },
                  "id": { "type": "string" },
                  "fileName": { "type": "string" },
                  "name": { "type": "string" },
                  "description": { "type": "string" },
                  "source": { "type": "string" }
                }
              }
            }
          }
        },
        "responses": {
          "201": { "description": "Created" },
          "400": { "description": "Invalid request" },
          "409": { "description": "Id or file already exists" }
        }
      }
    },
    "/api/v1/topology/{id}": {
      "put": {
        "tags": ["Topology"],
        "summary": "Upload a PDB topology directly",
        "parameters": [
          { "name": "id", "in": "path", "required": true, "schema": { "type": "string" } },
          { "name": "name", "in": "query", "required": false, "schema": { "type": "string" } },
          { "name": "description", "in": "query", "required": false, "schema": { "type": "string" } },
          { "name": "source", "in": "query", "required": false, "schema": { "type": "string" } },
          { "name": "fileName", "in": "query", "required": false, "schema": { "type": "string" } }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "chemical/x-pdb": { "schema": { "type": "string" } },
            "text/plain": { "schema": { "type": "string" } }
          }
        },
        "responses": {
          "201": { "description": "Created" },
          "400": { "description": "Invalid request" },
          "409": { "description": "Id or file already exists" },
          "413": { "description": "Upload exceeds configured limit" }
        }
      },
      "get": {
        "tags": ["Topology"],
        "summary": "Read a stored PDB topology file",
        "parameters": [
          { "name": "id", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": { "200": { "description": "PDB content", "content": { "chemical/x-pdb": {} } } }
      },
      "delete": {
        "tags": ["Topology"],
        "summary": "Delete a stored topology",
        "parameters": [
          { "name": "id", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": {
          "200": { "description": "Deleted" },
          "404": { "description": "Missing topology" }
        }
      }
    },
    "/api/v1/yaml": {
      "get": {
        "tags": ["YAML"],
        "summary": "List available YAML files",
        "responses": { "200": { "description": "YAML file names", "content": { "application/json": {} } } }
      }
    },
    "/api/v1/yaml/{name}": {
      "get": {
        "tags": ["YAML"],
        "summary": "Read a YAML file",
        "parameters": [
          { "name": "name", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": { "200": { "description": "YAML content", "content": { "text/yaml": {} } } }
      },
      "put": {
        "tags": ["YAML"],
        "summary": "Create a new YAML file",
        "parameters": [
          { "name": "name", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "text/yaml": { "schema": { "type": "string" } },
            "application/x-yaml": { "schema": { "type": "string" } },
            "text/plain": { "schema": { "type": "string" } }
          }
        },
        "responses": {
          "201": { "description": "Created" },
          "400": { "description": "Invalid YAML or rule violation" },
          "409": { "description": "File exists" }
        }
      },
      "post": {
        "tags": ["YAML"],
        "summary": "Update an existing YAML file",
        "parameters": [
          { "name": "name", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "text/yaml": { "schema": { "type": "string" } },
            "application/x-yaml": { "schema": { "type": "string" } },
            "text/plain": { "schema": { "type": "string" } }
          }
        },
        "responses": {
          "200": { "description": "Updated" },
          "400": { "description": "Invalid YAML or rule violation" },
          "404": { "description": "Missing file" }
        }
      },
      "delete": {
        "tags": ["YAML"],
        "summary": "Delete a YAML file",
        "parameters": [
          { "name": "name", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": {
          "200": { "description": "Deleted" },
          "404": { "description": "Missing file" }
        }
      }
    },
    "/api/v1/yaml/{name}/rename": {
      "post": {
        "tags": ["YAML"],
        "summary": "Rename a YAML file",
        "parameters": [
          { "name": "name", "in": "path", "required": true, "schema": { "type": "string" } },
          { "name": "to", "in": "query", "required": true, "schema": { "type": "string" } }
        ],
        "responses": {
          "200": { "description": "Renamed" },
          "400": { "description": "Invalid name" },
          "404": { "description": "Missing source" },
          "409": { "description": "Target exists" }
        }
      }
    }
  }
}
